# CNN plugin

Fetches news from CNN via RSS.

## Supported URL

`https://www.cnn.com`

## Options

- `feed` (string) — override the RSS feed URL. Defaults to `https://rss.cnn.com/rss/edition.rss`.

## Render output

`richText` — HTML and plain-text article summary from the RSS feed.
